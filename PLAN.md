# PLAN — QR Menu for Cafe: шлях до продакшену

Дорожня карта рефакторингу та доведення проєкту до продакшену. Оновлюється в міру виконання.

## Рішення (зафіксовані на інтерв'ю)

| # | Питання | Рішення |
|---|---------|---------|
| 1 | Backend | **Supabase** (Postgres + Auth + Storage + RLS) |
| 2 | Тип продукту | **Одне конкретне кафе** (без мульти-тенантності) |
| 3 | Авторизація власника | **Email + пароль** (Supabase Auth) |
| 4 | Деплой | **Vercel** |
| 5 | Фото | **Supabase Storage** |
| 6 | Оплата | **Офіціанту** (без онлайн-оплати) |
| 7 | Мови / валюта | **uk / en / hu**, гривня |
| 8 | Аналітика | **Базова** (окремим етапом після релізу) |
| 9 | Тести | **Unit + компонентні** (Vitest + React Testing Library) |
| 10 | Обсяг Фази 1 | **Повний перехід** (все одразу) |
| 11 | UI | **Зберегти стиль** (stone/amber), полірувати |
| 12 | Сповіщення | **Supabase Realtime + звук** у кабінеті |

## Поточний стан (аудит 2026-08-11)

- Next.js 15 App Router, React 19, Tailwind CSS 4, `motion`, `qrcode`; усе — клієнтські компоненти (`'use client'`).
- Усі дані в `localStorage` браузера з крос-таб синхронізацією (`storage` events). Бекенду немає.
- Тримовний словник `TRANSLATIONS` у `lib/store.tsx`, тримовна модель даних (`*Ua/*En/*Hu`).
- Залишки шаблонного коду вже прибрано (README переписано; `metadata.json`, `.eslintrc.json`, `bun.lock` видалено; непотрібні env-змінні та невикористана залежність прибрані).

### Відомі баги (черга на виправлення)
- [ ] Замовлення гостя не доходять до власника (localStorage — на пристрої гостя). Вирішується переходом на Supabase.
- [ ] Пароль власника захардкоджений `admin123` і показаний на екрані входу (`OwnerCabinet.tsx:323`, `login.default_notice`).
- [ ] Кнопка «Зберегти страву» поза `<form>` — валідація `required` не спрацьовує; можна зберегти порожні описи/фото та ціну 0 (`OwnerCabinet.tsx:1397`, `:405`).
- [ ] `parseInt` для ціни обрізає копійки (`OwnerCabinet.tsx:1162`).
- [ ] Будь-який `?table=X` приймається без перевірки; у гостя немає вибору столу (переклад `cart.choose_table` не використовується).
- [ ] Видалення категорії не переназначає її страви (лишаються з мертвим `category` id).
- [ ] Статуси замовлень можна перемикати в будь-якому порядку (без валідації переходів).
- [ ] QR на екрані — зовнішній API `api.qrserver.com`, а завантаження — локальний `qrcode` (розбіжність + зовнішній запит).
- [ ] Немає обробки помилок: надсилання замовлення без `try/catch`, QR-download мовчки логує.
- [ ] Захардкоджені українські рядки в кабінеті власника в обхід i18n (статистика, «Стіл #», «Загалом:», «Каса» та ін.).
- [ ] Мертвий код: невикористані імпорти (`Printer`, `Eye`, `TrendingUp`, `Maximize2`, `ChevronRight`, `ImageIcon`), подія `qr_menu_orders_updated` без слухачів, стан `initialized` завжди `true` (гарди в ефектах — no-op).

## Фаза 1 — «поставити на рельси»

### Етап 0 — Фундамент (без зовнішніх залежностей) — ✅ ВИКОНАНО
- [x] `git init`, оновити `.gitignore` (README вже переписано, шаблонний boilerplate прибрано).
- [x] Оновити `app/layout.tsx` metadata, `package.json` name, `metadata.json` (видалено).
- [x] Видалити мертвий код (невикористані імпорти, `initialized`, подія без слухачів).
- [x] TypeScript strict; ESLint у збірці (`prebuild`); чисті типи (прибрано `any`).
- [x] Налаштувати Vitest + React Testing Library + `npm test` script.
- [x] Перші тести: store (CRUD, ціни, статуси), `t()` — 19 тестів зелених.

### Етап 1 — Supabase (бекенд) — ✅ ВИКОНАНО
- [x] Створити проєкт Supabase (**QRMenu**, ref `qgdevkykhnjzdjpitnps`, eu-central-1, free).
- [x] Міграції: `categories`, `menu_items`, `tables`, `orders`, `order_items` (+ індекси, enum статусів, тригер updated_at).
- [x] RLS: публічне читання меню/категорій/столів; INSERT замовлень анонімно; керування — лише власник. Додатково: найменші привілеї anon (закрито TRUNCATE-дірку дефолтних грантів), security-адвайзер чистий.
- [x] Supabase Auth: email + пароль — налаштовано за замовчуванням; створення акаунта власника — на Етапі 2 разом з екраном входу.
- [x] Supabase Storage: bucket `dish-images` (public, 2MB, jpeg/png/webp), запис лише власник.
- [x] Realtime: публікація таблиці `orders`.

> Міграції у `supabase/migrations/` застосовано на віддалений проєкт (через MCP `apply_migration`); env-змінні у `.env.example`/`.env.local`.

### Етап 2 — Перенесення фронтенду — ✅ ВИКОНАНО
- [x] Замінити localStorage-стор на Supabase-клієнт (контекст `lib/store.tsx` — fetch, CRUD, realtime; без додаткових бібліотек стану).
- [x] Seed первинних даних (`supabase/seed.sql` — 5 категорій, 7 страв, 5 столиків).
- [x] Замовлення: INSERT через клієнт (uuid, резолюція label→uuid); кабінет підписаний на Realtime + звук.
- [x] Екран входу власника (Supabase Auth email+пароль, logout); акаунт власника створено (`etvesh.p@gmail.com`, тимчасовий пароль видано — змінити на постійний).
- [x] Завантаження фото → Supabase Storage (`dish-images`), пресети лишились.
- [x] Баги: збереження страви всередині `<form>` (валідація required), валідація ціни (Math.round + ≥0), валідація `?table=` (невідомий стіл → table_id null), валідація переходів статусів, QR генерується локально (`qrcode`), обробка помилок у мутаціях.

### Етап 3 — Якість — ✅ ВИКОНАНО
- [x] Увесь UI-текст через `t()` (статистика кабінету, титли/кнопки/плейсхолдери, привітання, 404; виняток — Suspense-fallback у `page.tsx`, поза провайдером).
- [x] Компонентні тести: кошик, оформлення замовлення (`?table=`), вхід власника, форма додавання страви. Спільний фейк Supabase у `lib/__fixtures__/`. Разом: 26 тестів.
- [x] `next build` без попереджень; TS strict + ESLint чисті.

### Етап 4 — Деплой (у процесі)
- [x] Репозиторій: GitHub `etveshp/qrmenu` — локальна історія запушена (`main`), `engines.node` закріплено.
- [x] Фікс збірки на Vercel: прибрано `output: 'standalone'` (Turbopack не генерує `.nft.json` — крок Vercel падав).
- [x] **Workflow preview-first**: Production Branch у Vercel = `production`; гілка `production` створена; перший прод-деплой запущено.
  - push у `main` → **preview**; реліз → PR `main` → `production` (або `git push origin main:production`, якщо `production` не розійшлася).
- [ ] Env-змінні у Vercel (`NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY`) — підтверджено: збірка пройшла, отже задані.
- [ ] Домен (опційно).
- [ ] Smoke-тест продакшену (HTTP-рівень ✅): **Production live = `https://qrmenu-omega-azure.vercel.app`** — `/` і `/?table=3` → 200, JS-чанки віддаються, Supabase anon-читання меню працює (7 страв). Інтерактивна перевірка (кошик/кабінет) — користувачем у браузері.
  - ⚠️ URL `qrmenu-7ng3wex5u-…` — deployment-посилання під захистом (302→SSO); справжній alias — `qrmenu-omega-azure.vercel.app`. `qrmenu.vercel.app` — зайнятий чужим проєктом.
- ✅ **Build-cache інцидент вирішено** (2026-08-12): Vercel збирав зі старого Turbopack-кешу (шрифти не оновлювались). Рішення: Deploy Hook на гілку `production` + `?buildCache=false` (або Redeploy → зняти «Use existing Build Cache»). Превентивно: `VERCEL_FORCE_NO_BUILD_CACHE=1` у env проєкту.

## Після релізу (окремі етапи)
- [ ] Базова аналітика продажів (виручка за період, кількість замовлень, топ-страв).
- [ ] Push-сповіщення на телефон власника (web push).
- [ ] PWA (офлайн, встановлюваність).
- [ ] Онлайн-оплата (LiqPay/Stripe/Portmone) — опційно.

## Потрібно від власника (по ходу)
- Доступ до Supabase (організація + підтвердження створення проєкту).
- Репозиторій (GitHub/GitLab) + акаунт Vercel.
- Email для акаунта власника в Supabase Auth.
